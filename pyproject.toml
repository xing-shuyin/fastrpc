[project]
name = "fastrpc-py"
version = "0.1.2"
authors = [{ name = "xingshuyin", email = "xing-shuyin@qq.com" }]
license = { text = "MIT" }
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
    "python-multipart>=0.0.20",
    "requests>=2.32.5",
    "setuptools>=80.9.0",
    "uvicorn[standard]>=0.37.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
